<!DOCTYPE html>
<html>
  <head>
    <title>Adc2019Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://use.typekit.net/bpv2qiz.css">

  </head>

  <body class="prototypes_<%= action_name %> <%= @prototype && @prototype.persisted? ? 'prototype_body_' + @prototype.id.to_s : '' %> noScrollBar">
    <%= yield %>
  </body>
</html>
